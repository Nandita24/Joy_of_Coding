//Abbreviations

#include<stdio.h>
#define MAX 1000

int main()
{
    char a[MAX];
    int i;
    printf("Enter the string\n");
    scanf("%[^\n]",a);


    int len=0;

    for(int i=0;a[i]!='\0';i++)
        len++;

    for(int i=0;a[i]!='\0';i++)
    {
        if(a[i]==' ')

    }
}
